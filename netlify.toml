[build]
  command = "npm run build"
  # Poprawiona ścieżka do katalogu generowanego przez `vite build` i `esbuild`
  publish = "dist"

# --- Przekierowania (pozostają bez zmian) ---
[[redirects]]
  from = "https://www.blackhole-universe.netlify.app/*"
  to = "https://blackhole-universe.netlify.app/:splat"
  status = 301
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# --- Nagłówki (pozostają bez zmian) ---
[[headers]]
  for = "/*"
  [headers.values]
    Vary = "Accept-Encoding"

[[headers]]
  for = "/*.html"
  [headers.values]
    Content-Type = "text/html; charset=utf-8"